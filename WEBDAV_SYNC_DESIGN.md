# WebDAV 同步方案设计

> 目标：在现有 Git 同步之外，增加一种基于 WebDAV 的同步方式，让用户不需要自建仓库、不污染提交历史、开箱即用。
> 状态：已按本文方案实现并配套自动化验证。基于上游 `3ac353e` 开发。

---

## 1. 为什么要做这个

现在的多设备同步走 Git：每台设备把自己的快照提交到一个私有仓库，再从仓库里读别人的。能用，但有三个实打实的问题。

**一、提交历史被污染。** 快照是"只有最新值有意义"的数据，历史没有任何价值。但 Git 会把每一次同步都记成一次提交。按 10 分钟一次、3 台设备算，一天 432 次提交，一个月一万三。仓库变成一条没人会看的流水账。

**二、多设备推送会互相顶掉。** 三台机器闭着眼往同一个分支推，晚到的那台会被拒绝，那一轮数据就丢了。为了绕开它，现在的实现里塞了一整套加锁、变基、审计、重试的逻辑（`SyncManager.swift` 里那段内嵌 shell 脚本，是这个文件 1135 行里的大头）。

**三、用户门槛高。** 想用这个功能，得先有一个私有 Git 仓库，还要给每台机器配好密钥。对一个菜单栏小工具来说，这个前置成本太重了。

### 关键观察

**这个场景天生没有冲突。** 每台设备只写自己那一个文件，永远不碰别人的。不存在两个人改同一处的情况。

也就是说，上面第二条里那一整套复杂度，是为了解决一个本来不存在的问题——它是 Git 的设计前提带进来的，不是这个场景需要的。

换成 WebDAV，那些逻辑不是被修好，是**不再需要存在**。

---

## 2. 已经验证过的前提

这几条决定了改造的边界，都在当前代码里确认过：

| 事实 | 位置 | 意味着 |
|---|---|---|
| `loadPeers()` 只是列目录 + 读 `*.json`，**零处涉及 git** | `SyncManager.swift` 的 `loadPeers`（243 行） | 换传输**不用碰展示层**。只要文件落到同步目录里，App 照常显示 |
| `synchronize()` 开头硬性检查 `.git` 目录，不是仓库直接返回 `invalidRepository` | `SyncManager.swift:1033` | 传输层是唯一要改的地方，且改动是隔离的 |
| `sync_interval` 单位是**分钟**，且只接受 `[30, 60, 120]`，填别的静默回落到 30 | `SyncManager.swift:8/88`、`main.swift:178` | 现在最快只能 30 分钟一次。WebDAV 便宜得多，可以放宽 |
| 采集脚本 `usage.30s.py` **零第三方依赖** | 全文件 | 无头节点的实现必须只用标准库 |
| 设备名允许空格和中文，只禁 `/ \ NUL` 和控制字符，上限 128 字符 | `SyncManager.swift` 的 `validDeviceID`（648 行）、`usage.30s.py:_sync_snapshot_filename` | 拼 URL 时必须做百分号转义 |
| 项目里目前**没有任何钥匙串代码**，配置全是明文 | 全项目 | WebDAV 密码需要新增安全存储 |

---

## 3. 设计目标

**要做到的**

- 用户只填「地址 + 用户名 + 密码 + 目录」四项就能用，不需要任何自建服务
- 不改动展示层（`loadPeers` / `PanelView` / 面板数据结构一律不动）
- Git 方式**继续保留**，默认不变，WebDAV 是可选项，不强制迁移
- 无头 Linux 节点同样支持，且不引入任何新依赖
- 能在坚果云免费额度内以 5 分钟的频率长期运行

**不做的**

- 不做端到端加密（App 需要能读明文才能展示，加密后就读不了了；真要防，见 §12 的裁剪方案）
- 不做历史版本、不做回滚（快照只有最新值有意义）
- 不做双向合并（不存在冲突，见 §7）
- 不做多人共享同一份数据（一个人的多台设备，不是多个人）

---

## 4. 整体架构

引入一层传输抽象，Git 和 WebDAV 各是一个实现：

```
                    ┌──────────────────────────┐
                    │  展示层（完全不动）        │
                    │  loadPeers() 列目录读 json │
                    └────────────▲─────────────┘
                                 │ 文件落到本地同步目录
                    ┌────────────┴─────────────┐
                    │      SyncBackend 协议     │
                    │  push() / pull() / probe()│
                    └──────┬────────────┬──────┘
                           │            │
                 ┌─────────▼──┐   ┌─────▼──────────┐
                 │ Git 后端    │   │ WebDAV 后端     │
                 │（现有代码）  │   │（新增）         │
                 └────────────┘   └────────────────┘
```

**本地目录布局保持完全一致。** 不管用哪个后端，`~/.tokei/sync/` 里都是一堆 `<设备名>.json`。这是整个设计最重要的一点——它保证了展示层零改动，也保证了两种方式可以随时互换。

协议大致长这样（示意）：

```swift
protocol SyncBackend {
    /// 把本机快照推上去
    func push(deviceID: String, localFile: URL) throws
    /// 拉取所有同伴的快照到本地同步目录，返回本次实际更新了哪些
    func pull(deviceID: String, into dir: URL) throws -> [String]
    /// 配置连通性自检，供设置界面的「测试连接」用
    func probe() throws -> ProbeResult
}
```

现有的 `GitSyncCode` 枚举里有一半是 git 专有的（`rebaseFailed`、`detachedHead`、`dirtyRepository`…）。需要抽出一组通用错误码（`authFailed` / `networkFailed` / `quotaExceeded` / `notFound` / `busy` / `success`），git 专有的降级成 detail 文本。

---

## 5. 远端布局

```
<WebDAV 根>/tokei/
    ├── Avz-Studio.json.gz
    ├── OCI-Phoenix.json.gz
    └── LAN-RTOS.json.gz
```

**一台设备 = 一个文件，文件名就是设备名。** 和现在的本地布局一一对应。

### 为什么要压缩

这是这个方案能不能成立的分水岭。实测本项目的真实数据：

| 设备 | 原始 | gzip 后 | 压缩比 |
|---|---|---|---|
| Mac（176 天历史） | 122 KB | **13 KB** | 9.4× |
| 内网节点 | 25 KB | 3 KB | 8.3× |
| 凤凰城节点 | 22 KB | 2 KB | 11× |

压得这么狠是因为快照是一大坨结构高度重复的 JSON。其中 Mac 那 122KB 里有 **98KB（80%）是 `_dashboard`**——给面板画图表用的每日历史和 wrapped 数据。

**注意：要在文件层面压缩，存成 `.json.gz`，不要指望 HTTP 的内容编码协商**——WebDAV 服务器对这个的支持很不一致，靠不住。

本地仍然存解压后的 `.json`。顺带一个安全副作用：`loadPeers()` 匹配的是 `.json` 后缀，`X.json.gz` 不匹配，所以即使有 `.gz` 残留在目录里也不会被误读。不过状态文件还是建议放到同步目录外（见 §6）。

---

## 6. 数据流

### 6.1 Mac 端（有界面，需要看全部设备）

```
定时器触发
  ├─ 采集：调 usage.30s.py --write-sync  →  写出本机 <设备名>.json
  ├─ 上传：gzip → PUT /tokei/<设备名>.json.gz
  └─ 拉取：PROPFIND /tokei/ (Depth:1)
           对每个同伴，比对 ETag 和本地记录
             ├─ 没变 → 跳过，不下载
             └─ 变了 → GET → gunzip → 写入本地同步目录
        刷新界面
```

### 6.2 无头 Linux 节点（没有界面）

```
crontab 触发
  ├─ 采集：usage.30s.py --write-sync
  └─ 上传：gzip → PUT /tokei/<设备名>.json.gz
```

**就这两步，没有拉取。**

这是相对 Git 的一个实打实的改进：无头节点根本不需要知道别人的数据，它没有界面，拉回来也没人看。但 Git 强迫它必须 `git pull` 整个仓库才能推送。WebDAV 下它只管上传，**流量直接省掉一半，逻辑也简单一半**。

### 6.3 ETag 状态存哪

```
~/.tokei/webdav-state.json     # 注意：在同步目录之外
{
  "peers": {
    "OCI-Phoenix": {"etag": "\"a1b2c3\"", "fetched_at": 1785551234},
    "LAN-RTOS":    {"etag": "\"d4e5f6\"", "fetched_at": 1785551190}
  }
}
```

放在同步目录外面，避免和快照文件混在一起。

---

## 7. 冲突处理：不需要

**每台设备只写自己那一个远端文件，永远不写别人的。** 所以远端不存在两方写同一个文件的情况，也就不需要锁、不需要变基、不需要冲突解决、不需要失败重试的退避策略。

唯一真实存在的竞争是：**同一台机器上两个进程同时同步**（比如定时器和手动触发撞上了）。这个用现有的本地锁就能解决，保留即可。

对比一下现在 Git 后端为了应付冲突所必须做的事：抢仓库锁 → fetch → 检查是否有外来操作 → 变基 → 审计变基结果只动了自己的文件 → 推送 → 失败则重来。这一整串在 WebDAV 下全部消失。

---

## 8. 失败语义

| 情况 | 行为 | 理由 |
|---|---|---|
| 上传失败 | 保留本地文件，下一轮重试 | 无状态可损坏。不像 Git 失败后本地会留着未推送的提交，下次得先处理 |
| 某个同伴下载失败 | 保留该同伴上一次的本地副本，界面上按"数据偏旧"呈现 | 已有 `_ts` 字段可以算出新鲜度，展示层不用改 |
| 认证失败 | 立刻停止本轮，界面明确提示"用户名或密码错误"，**不重试** | 重试只会更快耗尽服务商的请求配额 |
| 超出流量配额 | 明确提示并建议降低频率，**暂停自动同步直到用户确认** | 坚果云超额会直接拒绝，闷头重试没有意义 |
| PROPFIND 成功但某个文件损坏 | 跳过该同伴并记进 issues，其余正常 | 复用现有 `PeerLoadIssue` 机制 |

---

## 9. 配置设计

### 9.1 config.json 扩展

保持向后兼容，不写 `sync_backend` 时一律当作 `git`：

```json
{
  "device_id": "Avz-Studio",
  "sync_dir": "~/.tokei/sync",
  "auto_sync": true,
  "sync_interval": 30,

  "sync_backend": "webdav",
  "webdav": {
    "url": "https://dav.jianguoyun.com/dav/",
    "path": "tokei",
    "username": "you@example.com",
    "compress": true
  }
}
```

**密码不写进这个文件。**

### 9.2 密码存哪

- **Mac 端**：存 macOS 钥匙串。项目目前完全没有钥匙串相关代码，需要新增一个很小的封装（`SecItemAdd` / `SecItemCopyMatching`），大约几十行。
- **无头 Linux 节点**：没有钥匙串。用环境变量 `TOKEI_WEBDAV_PASSWORD`，或者单独一个 `~/.tokei/webdav-secret` 文件并强制校验权限为 `600`（权限不对就拒绝启动并提示）。

坚果云这类服务用的是**单独生成的应用授权密码**，不是账号主密码，泄露的影响面可控——但这不是偷懒的理由，该存钥匙串还是要存。

### 9.3 设置界面

在现有的「多设备同步」设置区（`PanelView.swift` 的 `settingsSyncSection`，1780 行）加一个方式选择：

```
同步方式   ( ) Git 仓库      (•) WebDAV

  服务器地址  [https://dav.jianguoyun.com/dav/        ]
  目录        [tokei                                  ]
  用户名      [you@example.com                        ]
  密码        [••••••••••••                           ]
  □ 压缩后上传（推荐，可省约 90% 流量）

  [ 测试连接 ]   ← 走 probe()，验证可连、可写、可列目录
```

「测试连接」这个按钮很重要。WebDAV 的配置错误（地址少了尾斜杠、目录不存在、用了主密码而不是应用密码）都是静默失败，没有它用户会一头雾水。

### 9.4 放宽同步间隔

现在限定 `[30, 60, 120]` 分钟，这个限制是给 Git 定的——Git 同步重且有风险，太频繁不合适。

WebDAV 一轮就是几个 HTTP 请求，建议按后端区分：

| 后端 | 可选间隔 |
|---|---|
| Git | 30 / 60 / 120 分钟（维持现状） |
| WebDAV | **5** / 15 / 30 / 60 / 120 分钟 |

流量测算见下一节，5 分钟这一档在坚果云免费额度内非常宽裕。

---

## 10. 流量与配额

坚果云免费版的硬限制（[官方帮助中心](https://help.jianguoyun.com/?tag=webdav)）：

- 每月上传 **1 GB**、下载 **3 GB**
- 每 30 分钟不超过 **600 次请求**
- 单文件上限 500 MB，单次列目录上限 750 个条目

按本项目真实数据测算（3 台设备：1 台 Mac 拉取 2 个同伴，2 台无头只上传）：

| 频率 | 每月上传 | 每月下载 | 是否够用 |
|---|---|---|---|
| **不压缩**，5 分钟 | 1.46 GB | 406 MB | ❌ 上传超额 |
| 压缩，30 分钟 | 26 MB | 7 MB | ✅ 非常宽裕 |
| 压缩，15 分钟 | 52 MB | 14 MB | ✅ 宽裕 |
| **压缩，5 分钟** | **155 MB** | **43 MB** | ✅ 推荐 |
| 压缩，1 分钟 | 778 MB | 216 MB | ⚠️ 能跑但接近上限 |

请求次数：一轮里 Mac 是 1 次列目录 + 最多 2 次下载 + 1 次上传 = 4 次，两个无头节点各 1 次上传，合计 6 次。5 分钟一轮的话每 30 分钟 36 次，离 600 的限制很远。

**两个要点：**

1. **限制是按账号算的，多台设备共用一个账号，要算总和。** 设备越多越要注意。
2. 加上 ETag 条件下载后，同伴没变化的轮次不产生下载流量，实际用量会明显低于上表。

---

## 11. 各家 WebDAV 的兼容性

不同实现之间差异不小，这几个坑要在实现时处理掉：

- **尾斜杠**：目录 URL 有没有尾斜杠，不同服务器行为不同。规范化：内部一律补齐尾斜杠再拼路径。
- **列目录深度**：必须显式带 `Depth: 1`，有的服务器默认 `infinity` 会返回一大坨。
- **目录不存在**：先 `MKCOL` 建目录，已存在会返回 405，要当成正常情况而不是错误。
- **属性名带命名空间**：解析返回的 XML 时要按命名空间取 `getetag` / `getlastmodified`，不能只按标签名硬匹配。
- **ETag 格式不统一**：有的带引号有的不带，有的带 `W/` 弱校验前缀。只做原样字符串比对，不要试图解析语义。
- **设备名要转义**：设备名允许空格和中文，拼进 URL 前必须做百分号编码。
- **不要依赖 HTTP 内容编码协商**（见 §5）。

建议在 README 里给出几个已验证可用的服务，至少覆盖：坚果云、Nextcloud、群晖，以及自建（如 rclone serve webdav）。

---

## 12. 安全

**传输**：强制 HTTPS。配置里填了 `http://` 应当直接拒绝并提示，除非是 `localhost`。

**存储**：这里有一件事值得单独说清楚。

快照里**包含项目名和每个项目的花费**。实测本机数据里有 `kmpsecu365`、`bleDemo-webui-to-compose`、`BleDemo` 这类名字，还带着各自烧了多少钱。这比 token 数字本身敏感——它会泄露你在做什么项目。

对多数人来说，放在自己的网盘账号下、用独立的应用密码，到这个程度就够了。但既然做成公开功能，建议加一个开关：

```
□ 上传时移除项目名（只保留汇总数据）
```

打开后，上传前把 `_dashboard.wrapped.*.projects` 这一段抹掉。代价是"跨设备看项目排行"这一个展示功能失效，其他都不受影响。

**不做端到端加密的理由**：App 必须能读明文才能展示。真加密了，要么密钥得跟着同步（等于没加密），要么用户每台设备手工输一遍密钥（体验崩掉）。收益和成本不成比例。

---

## 13. 采集脚本的改造（无头节点）

`usage.30s.py` 现在是**零第三方依赖**的单文件脚本，这个性质必须保住——它靠 `curl` 一行就能装到任意一台服务器上。

好消息是标准库完全够用：

- `urllib.request` 支持自定义方法，`PUT` 和 `PROPFIND` 都能发
- `base64` 拼 Basic 认证头
- `gzip` 压缩
- `xml.etree.ElementTree` 解析列目录返回

新增一个命令行开关：

```bash
python3 usage.30s.py --sync-push     # 采集 + 上传，无头节点用这一个就够
```

它读 `config.json` 里的 `sync_backend`：
- 是 `webdav` → 采集后直接上传
- 是 `git` 或没配 → 保持现在的行为（只写本地文件），交给外部脚本处理

**这样无头节点的部署会比现在简单得多**：不用 clone 仓库、不用配部署密钥、不用写包装脚本处理推送重试。一行 crontab 调 `--sync-push` 就完了。

README 里那段 Linux 配置示例可以相应大幅简化。

---

## 14. 迁移与共存

**不强制迁移，不做自动转换。**

- 默认后端仍是 Git，老用户升级后行为完全不变
- 想换的人在设置里切一下，填好 WebDAV 信息即可
- 因为本地目录布局两边一致，切换后已有的同伴数据还在，界面不会突然空掉
- 切走之后原来那个 Git 仓库不动它，用户想删自己删

一个体验细节：切换后第一次同步前，界面上同伴数据的时间戳还是旧的。应当明确提示"正在首次同步"，而不是让用户以为坏了。

---

## 15. 测试计划

项目已有 `tests/test_sync_safety.py` 和 `Tokei/Tests/SyncManagerIntegrationCheck.swift`，沿用同样的路子。

**Python 侧**（新增 `tests/test_webdav_sync.py`）
- 起一个本地 WebDAV 假服务器（标准库 `http.server` 自己实现 PUT/PROPFIND 即可，不引依赖）
- 覆盖：正常上传、压缩往返一致性、认证失败、目录不存在自动创建、设备名含空格和中文的转义、密钥文件权限不对时拒绝启动

**Swift 侧**（扩展现有 integration check）
- 覆盖：ETag 命中时跳过下载、同伴文件损坏时不影响其他同伴、网络超时后本地数据保持不变、`probe()` 对各种错误配置给出正确的分类

**手工验证清单**
- 至少在坚果云 + 一个自建服务上各跑通一次
- 断网 → 恢复，确认能自愈
- 故意填错密码，确认提示明确且不会疯狂重试

---

## 16. 建议分两个 PR

一次性提交改动太大，评审负担重，容易卡住。建议拆开：

**PR 1：抽出传输抽象层（纯重构，行为零变化）**
- 定义 `SyncBackend` 协议
- 把现有 git 逻辑原样搬到 `GitSyncBackend` 里
- 抽出通用错误码，git 专有的降级成 detail
- 现有测试全部保持通过
- 好处：这个 PR 不改任何行为，评审起来快，也给作者一个表态的机会

**PR 2：WebDAV 后端**
- `WebDAVSyncBackend` 实现
- 钥匙串封装
- 设置界面 + 测试连接
- 采集脚本 `--sync-push`
- 按后端区分同步间隔
- 新增测试 + README 文档

---

## 17. 需要和作者确认的问题

1. **作者是否接受引入第二种传输方式？** 这是最根本的一条，值得先开个 issue 问，而不是直接甩 PR。
2. 是否愿意接受**放宽同步间隔到 5 分钟**（仅 WebDAV）？
3. 钥匙串是新引入的依赖，作者对此有没有顾虑（比如签名、权限描述文件）？
4. 「移除项目名」这个开关，是做成 WebDAV 专属，还是对 Git 方式也一并提供？（我倾向于都提供——Git 那边同样会把项目名传到远端仓库。）
5. 无头节点的密码，环境变量和文件两种方式都支持，还是只留一种？

---

## 附：本文涉及的关键代码位置

| 内容 | 文件 | 行 |
|---|---|---|
| 同步配置结构 | `Tokei/Sources/Tokei/SyncManager.swift` | 4–9 |
| 间隔可选值与默认值 | 同上 | 88–89 |
| 设备名合法性校验 | 同上 | 648 |
| Git 事务脚本（要搬走的大头） | 同上 | `transactionScript` 661、`transactionSupervisorScript` 923 |
| 读取同伴数据（**不要动**） | 同上 | `loadPeers` 243 |
| 硬性检查 `.git` 的地方 | 同上 | 1033 |
| 自动同步定时器 | `Tokei/Sources/Tokei/main.swift` | 178 |
| 设置界面「多设备同步」区块 | `Tokei/Sources/Tokei/PanelView.swift` | `settingsSyncSection` 1780 |
| 快照写入 | `usage.30s.py` | `_write_sync_snapshot` |
| 设备名转文件名 | `usage.30s.py` | `_sync_snapshot_filename` |
